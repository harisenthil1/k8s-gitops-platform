replicaCount: 2

image:
  repository: ghcr.io/harisenthil1/k8s-gitops-platform
  tag: "694ac54549af68cae5eb927a96035f88a547b6db"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 60

secret:
  appApiKey: "demo-secret-change-me"

networkPolicy:
  enabled: true
